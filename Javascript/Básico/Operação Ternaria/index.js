//Se a condição for verdadeira será atribuida a informação após o "?"
//Se a condição for Falsa será atribuida a informação após o ":"

const pontos = 1000

const usuario = pontos >= 1000 ? 'VIP' : 'Normal'

console.log(usuario)